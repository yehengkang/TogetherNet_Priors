- 脚本以 `if __name__ == "__main__":` 包裹全部逻辑，直接通过 Python 解释器运行而非 import。
- 路径采用硬编码相对路径常量（如 `VOCdevkit_path = 'RTTStest'`、`map_out_path = 'map_out'`），通过 `os.path.join` 拼接，不使用 argparse 或配置文件。
- 对数据集目录缺失的情况显式 `os.makedirs` 创建 `map_out/{ground-truth,detection-results,images-optional}` 三级子目录。