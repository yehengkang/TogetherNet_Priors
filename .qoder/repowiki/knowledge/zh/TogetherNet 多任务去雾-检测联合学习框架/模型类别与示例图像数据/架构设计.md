本模块为纯数据资源目录，不包含任何代码逻辑：
- `model_data/` 提供三类数据集的类别名映射文件，每行一个类别字符串，分别对应 COCO（80 类）、自定义 RTTS（5 类）和 VOC（20 类），供上层检测模型加载类别表时使用。
- `img/` 与 `img_out/` 作为占位目录，前者放置待推理的测试图片，后者由运行脚本写入可视化结果图，二者通过 `readme.txt` 提示用途。
依赖方向单向：上层 Python 脚本读取这些文本/图像文件，本模块不反向依赖任何代码。