`utils_coco/coco_annotation.py` 需先准备 `coco_dataset/{train2017,val2017}` 与 `annotations/instances_{train,val}2017.json`，运行后在仓库根生成 `coco_train.txt`/`coco_val.txt`；`utils_coco/get_map_coco.py` 通过 `map_mode` 控制是否只跑推理(`1`)、只跑评测(`2`)或两者都跑(`0`)，结果默认落盘至 `map_out/coco_eval/eval_results.json`。